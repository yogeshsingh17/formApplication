import { createContext } from "react";

export const FormContext = createContext({});           /* Creates a context object. This context object will
                                                           us to maintain a global data store. */